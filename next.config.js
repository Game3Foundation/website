/** @type {import('next').NextConfig} */
const nextConfig = {}

// No specific changes needed in next.config.js for next-sitemap
// The plugin works via the postbuild script and its own config file

module.exports = nextConfig
